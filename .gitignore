__pycache__/
*.pyc
venv/
.venv/
.env
*.key
*.log
framework/outbox/
framework/intake_manifest.json
node_modules/
*.zip

# operator-forged agents stay local
agents/
framework/agents/
collaboration/memory.jsonl
mindbot.cmd

# tts samples / generated audio (artifacts, not source)
voxx_sample.wav
*.wav

# runtime caches + derived artifacts
.free_models_cache.json
framework/mindbot_pipeline/.free_models_cache.json
harvest/
collaboration/digest_*.md
# commerce runtime data (orders/sales ledger is local state, not source)
framework/mindbot_pipeline/commerce/
# operational logs (the unattended-run trace; rotates locally, not source)
framework/mindbot_pipeline/logs/
# ledger chain-head sidecar (derived cache; rederivable from the ledger itself)
collaboration/*.head
*.jsonl.head
# generated attestation certs (regenerated by `mindbot attest`)
collaboration/ATTESTATION.json
collaboration/ATTESTATION.txt

# personal — never pushed
PRIVATE/
# promo build intermediates (regenerate with build_promo.py)
apps/promo/work/
apps/promo/qa/

# server run log (transient)
framework/serve.log
apps/intro_video/out/
